name: Dev Pipeline

on:
  push:
    branches: [dev]

  pull_request:
    branches: [dev]

env:
  DOTNET_VERSION: "9.0.x"

jobs:
  prepare:
    name: Prepare Environment 
    uses: actions/prepare/action.yml@dev

  build:
    name: Build Tasks.API
    needs: prepare
    uses: actions/build/action.yml@dev
    with:
      project-dir: src/Tasks/Tasks.API

  unit-tests:
    name: Run Unit Tests
    needs: build
    uses: actions/test/action.yml@dev
    with:
      test-project-dir: src/Tasks/Tasks.UnitTests
